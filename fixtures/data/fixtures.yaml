include:
    - templates.yaml

Domain\Link\Entity\Link:
    link{1..500} (extends link):

Domain\Authentication\Entity\User:
    user_admin (extends user):
        username: admin
        email: admin\@admin.com
        roles: ['ROLE_ADMIN']

    user{1..10} (extends user):
